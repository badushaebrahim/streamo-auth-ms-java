server:
  port: 7832
  servlet:
    context path: /auth-management/v1

spring:
  mvc:
    path match:
      matching-strategy: ant-path-matcher

  data:
    mongodb:
#       user: badusha
#       password: admin
      database: authorization
      uri: mongodb+srv://use:use@cluster0.lmat7tv.mongodb.net/?retryWrites=true&w=majority

app:
  name: spring boot application
  api:
    title: Spring Boot APIs
    version: 1.0.0
    description: Spring Boot APIs description
    base-package: com.abc.controller
    contact-name: apisupportgroup
    contact-email: apisupportgroup@abc.com
    swagger:
      enable: true

config:
  rest:
    services:
      encryptionKey: "keycodevalue"
